# Environment Configuration for AIReady Platform
# 
# SST uses stage-specific environment files:
# - .env.local  → for local development (sst dev --stage local)
# - .env.dev    → for deployed dev environment (sst deploy --stage dev)
# - .env.prod   → for production (sst deploy --stage prod)
#
# Copy this file to the appropriate .env.{stage} file and fill in values.

# =============================================================================
# Auth (NextAuth v5 variable names)
# =============================================================================
AUTH_SECRET=your-secret-here
AUTH_URL=http://localhost:8888

# =============================================================================
# GitHub OAuth
# =============================================================================
# Each environment needs its own GitHub OAuth app (GitHub only allows 1 callback URL per app)
# Local:    http://localhost:8888/api/auth/callback/github
# Dev:      https://dev.platform.getaiready.dev/api/auth/callback/github
# Prod:     https://platform.getaiready.dev/api/auth/callback/github
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# =============================================================================
# Google OAuth
# =============================================================================
# Google allows multiple redirect URIs per app, so one app can serve all environments:
# - http://localhost:8888/api/auth/callback/google
# - https://dev.platform.getaiready.dev/api/auth/callback/google
# - https://platform.getaiready.dev/api/auth/callback/google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# AWS (set via SST)
# =============================================================================
AWS_REGION=ap-southeast-2
DYNAMO_TABLE=aiready-platform-MainTable
S3_BUCKET=aiready-platform-AnalysisBucket
ANALYSIS_QUEUE_URL=your-sqs-queue-url

# =============================================================================
# Stripe
# =============================================================================
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
STRIPE_PRICE_ID_PRO=price_xxx
STRIPE_PRICE_ID_ENTERPRISE=price_xxx

# =============================================================================
# App
# =============================================================================
NEXT_PUBLIC_APP_URL=http://localhost:8888

# =============================================================================
# Cloudflare DNS (for custom domain)
# =============================================================================
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id
CLOUDFLARE_ZONE_ID=your-cloudflare-zone-id

# =============================================================================
# AI Models (MiniMax, OpenAI, etc.)
# =============================================================================
MINIMAX_API_KEY=your-minimax-api-key
MINIMAX_MODEL=MiniMax-M2.7
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# Sentry
# =============================================================================
SENTRY_AUTH_TOKEN=your-sentry-auth-token
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
